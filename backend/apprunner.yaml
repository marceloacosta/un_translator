version: 1.0
runtime: python311
build:
  commands:
    - pip3 install -r requirements.txt
run:
  command: python3 main.py
  network:
    port: 8080
  env:
    - name: AWS_REGION
      value: "us-east-1"
